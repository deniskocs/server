# Скопируйте в config.yaml. Файл не коммитится.
#
# root — каталог библиотеки на шаре (как BOOKS_LIBRARY_ROOT в Docker smb-books).
# Все пути инструментов — относительно этого каталога.

host: "10.0.0.1"
user: "smbuser"
password: "secret"
share: "public"
root: "Книги/books"

# port: 445
