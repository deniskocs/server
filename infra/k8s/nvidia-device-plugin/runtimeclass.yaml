# k3s подхватывает handler "nvidia" из containerd, если на ноде установлен nvidia-container-runtime.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
  annotations:
    argocd.argoproj.io/sync-wave: "-5"
handler: nvidia
