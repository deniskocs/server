# Helm chart не создаёт namespace при helm template; CreateNamespace в Application — только для destination (default).
apiVersion: v1
kind: Namespace
metadata:
  name: cert-manager
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
